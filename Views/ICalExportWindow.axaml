<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="500" d:DesignHeight="480"
        x:Class="YATODOL.Views.ICalExportWindow"
        Title="Export to iCal" Width="500" Height="480"
        CanResize="False"
        WindowStartupLocation="CenterOwner"
        Icon="avares://YATODOL/app-icon.png">

    <DockPanel Margin="24">
        <!-- Title -->
        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,16">
            <TextBlock x:Name="ICalTitleText" Text="📅 Export to iCal" FontSize="22" FontWeight="Bold"/>
            <TextBlock x:Name="ICalSubtitleText" Text="Select tasks to export as .ics calendar events"
                       Foreground="Gray" FontSize="13" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal"
                    HorizontalAlignment="Right" Spacing="10" Margin="0,16,0,0">
            <Button x:Name="ICalCancelButton" Content="Cancel" Click="OnCancelClick" Width="90" Padding="12,8"/>
            <Button x:Name="ICalExportButton" Content="Export" Click="OnExportClick" Classes="accent" Width="90" Padding="12,8"/>
        </StackPanel>

        <!-- Quick select -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Spacing="10" Margin="0,0,0,12">
            <Button x:Name="SelectAllButton" Content="Select All" Click="OnSelectAll" Padding="8,4" FontSize="12"/>
            <Button x:Name="SelectNoneButton" Content="Select None" Click="OnSelectNone" Padding="8,4" FontSize="12"/>
            <Button x:Name="UncompletedOnlyButton" Content="Uncompleted Only" Click="OnSelectUncompleted" Padding="8,4" FontSize="12"/>
        </StackPanel>

        <!-- Task list -->
        <Border BorderBrush="#30888888" BorderThickness="1" CornerRadius="8" Padding="4">
            <ScrollViewer>
                <StackPanel x:Name="TaskListPanel" Spacing="2"/>
            </ScrollViewer>
        </Border>
    </DockPanel>
</Window>
